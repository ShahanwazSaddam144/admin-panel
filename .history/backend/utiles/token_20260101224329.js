const jwt = require("jsonwebtoken")

const JWT_SECRET = process.env.JWT_SECRET

export const verifyToken=(token)=>{
    try{
        const token
    }
    catch(e){
        console.log(e)
        return null
    }
}